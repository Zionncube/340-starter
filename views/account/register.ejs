<!-- filepath: c:\Users\user\Desktop\340-starter\views\account\register.ejs -->
<%- include('../partials/head') %>
<%- include('../partials/header') %>
<%- include('../partials/nav') %>
<main>
  <h1>Register</h1>
  <% if (errors && errors.length > 0) { %>
    <div class="flash-message error">
      <% errors.forEach(function(error) { %>
        <p><%= error.msg %></p>
      <% }) %>
    </div>
  <% } %>
  <% if (message && message.length > 0) { %>
    <div class="flash-message"><%= message %></div>
  <% } %>
  <form action="/account/register" method="post">
    <label for="account_firstname">First Name</label>
    <input type="text" id="account_firstname" name="account_firstname" required value="<%= typeof account_firstname !== 'undefined' ? account_firstname : '' %>">

    <label for="account_lastname">Last Name</label>
    <input type="text" id="account_lastname" name="account_lastname" required value="<%= typeof account_lastname !== 'undefined' ? account_lastname : '' %>">

    <label for="account_email">Email</label>
    <input type="email" id="account_email" name="account_email" required value="<%= typeof account_email !== 'undefined' ? account_email : '' %>">

    <label for="account_password">Password</label>
    <input type="password" id="account_password" name="account_password" required>
    <small>Password must be at least 12 characters and include uppercase, lowercase, number, and symbol.</small>

    <label for="account_type">Account Type</label>
    <select id="account_type" name="account_type" required>
      <option value="Client" <%= account_type === 'Client' ? 'selected' : '' %>>Client</option>
      <option value="Employee" <%= account_type === 'Employee' ? 'selected' : '' %>>Employee</option>
      <option value="Admin" <%= account_type === 'Admin' ? 'selected' : '' %>>Admin</option>
    </select>

    <button type="submit">Register</button>
  </form>
  <p>Already have an account? <a href="/account/login">Login here</a></p>
</main>
<%- include('../partials/footer') %>